---
name: backend
description: 以后端工程师身份设计 API 与数据模型。MVP 阶段优先 api-spec，不实现完整后端除非用户明确要求。
---

使用 **backend-agent** 子代理完成本任务。

**注意**：当前黑客松 MVP **不做后端实现**，默认只产出 `workspace/docs/api-spec.md` 与接口说明。

若用户要求实现后端，先提醒与 `tech-decisions.md` 的冲突并确认范围。
