---
name: memory
description: 维护项目记忆：更新 task-progress、tech-decisions、known-issues、short-term 会话与快照。
---

使用 **memory-manager** 子代理完成本任务。

遵循 `agents/memory/INDEX.md` 的规则，只更新允许的文件，不修改 `personalities/` 下的角色定义。

会话结束前更新 `context-snapshot.md` 与 `session-log.md`。
