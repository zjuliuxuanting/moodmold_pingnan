{
  "buildCommand": "cd workspace/frontend && npm install && npm run build",
  "outputDirectory": "workspace/frontend/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
